<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Priyanshu — Portfolio Signup</title>
  <style>
    :root{
      --bg1: #f0f8ff;
      --accent: #5b21b6; /* indigo */
      --accent-2: #06b6d4; /* teal */
      --glass: rgba(255,255,255,0.7);
      --muted: #6b7280;
      --card: rgba(255,255,255,0.85);
    }

    /* Reset */
    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%;font-family:Inter,ui-sans-serif,system-ui,Segoe UI,Roboto,"Helvetica Neue",Arial;}
    body{
      background: radial-gradient(1200px 600px at 10% 10%, #e6f7ff 0%, transparent 18%),
                  radial-gradient(900px 400px at 95% 85%, #f3e8ff 0%, transparent 18%),
                  linear-gradient(180deg,#ffffff 0%, #f8fafc 100%);
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:32px;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
    }

    /* Container card */
    .card{
      width:100%;
      max-width:1100px;
      display:grid;
      grid-template-columns:1fr 480px;
      background: linear-gradient(180deg, rgba(255,255,255,0.85), rgba(255,255,255,0.80));
      border-radius:18px;
      box-shadow: 0 12px 40px rgba(12,18,36,0.12);
      overflow:hidden;
      position:relative;
    }

    /* Left hero */
    .hero{
      padding:48px;
      position:relative;
      min-height:420px;
      display:flex;
      flex-direction:column;
      justify-content:center;
      gap:18px;
      overflow:hidden;
    }

    .title{
      font-size:34px;
      line-height:1.02;
      font-weight:800;
      color:#0f172a;
    }
    .title .name{color:var(--accent);}

    .lead{
      color:var(--muted);
      max-width:62ch;
      margin-top:8px;
      font-size:15px;
    }

    .cta-row{margin-top:18px;display:flex;gap:12px;align-items:center}
    .btn{
      display:inline-flex;
      align-items:center;
      gap:8px;
      padding:10px 16px;
      border-radius:10px;
      font-weight:600;
      cursor:pointer;
      text-decoration:none;
      user-select:none;
      border: none;
    }
    .btn-primary{
      background:linear-gradient(90deg,var(--accent), #7c3aed);
      color:white;
      box-shadow: 0 6px 18px rgba(99,102,241,0.20);
      transform:translateY(0);
      transition:transform .18s ease, box-shadow .18s ease;
    }
    .btn-primary:hover{ transform:translateY(-4px); box-shadow:0 18px 30px rgba(99,102,241,0.18) }
    .btn-ghost{
      background:transparent;
      color:var(--accent);
      border:1px solid rgba(99,102,241,0.12);
      padding:8px 14px;
    }

    /* Floating decorative shapes */
    .blob{
      position:absolute;
      filter: blur(30px);
      opacity:0.85;
      transform-origin:center;
    }
    .blob.b1{ width:340px;height:340px; right:-120px; top:-100px; background: radial-gradient(circle at 30% 30%, #c7b3ff, #8b5cf6); animation:spin 18s linear infinite;}
    .blob.b2{ width:240px;height:240px; left:-90px; bottom:-80px; background: radial-gradient(circle at 60% 40%, #99f6e4, #06b6d4); animation:float 6s ease-in-out infinite;}
    .blob.b3{ width:120px;height:120px; right:36px; bottom:40px; background: radial-gradient(circle at 20% 20%, #fde68a, #f59e0b); animation:pulse 4s ease-in-out infinite;}

    @keyframes spin{ from{transform:rotate(0deg)} to{transform:rotate(360deg)}}
    @keyframes float{ 0%{transform:translateY(0)}50%{transform:translateY(-18px)}100%{transform:translateY(0)}}
    @keyframes pulse{ 0%{transform:scale(1)}50%{transform:scale(1.08)}100%{transform:scale(1)} }

    /* Right signup */
    .signup{
      padding:36px;
      background:linear-gradient(180deg, rgba(255,255,255,0.88), rgba(250,250,255,0.85));
      display:flex;
      flex-direction:column;
      justify-content:center;
      gap:14px;
    }
    .h2{font-size:20px;font-weight:700;color:#0f172a}
    .muted{color:var(--muted);font-size:13px}

    form{display:grid;gap:12px;margin-top:6px}
    label{font-size:13px;color:#111827;font-weight:600}
    .input{
      display:flex;flex-direction:column;
    }
    input[type="text"], input[type="email"], input[type="password"]{
      padding:12px 14px;border-radius:10px;border:1px solid rgba(15,23,42,0.06);background:transparent;font-size:14px;outline:none;
      transition:box-shadow .14s ease, border-color .14s ease, transform .14s ease;
    }
    input:focus{ box-shadow: 0 10px 20px rgba(99,102,241,0.06); border-color: rgba(99,102,241,0.30); transform:translateY(-2px) }
    .small{font-size:12px;color:var(--muted);margin-top:6px}

    .checkbox-row{display:flex;gap:10px;align-items:center}
    .submit{
      margin-top:6px;
      padding:12px 14px;border-radius:10px;border:none;font-weight:700;background:linear-gradient(90deg,var(--accent), #7c3aed);color:white;cursor:pointer;
      box-shadow: 0 6px 18px rgba(99,102,241,0.12);
      transition:transform .14s ease, box-shadow .14s ease;
    }
    .submit:hover{ transform:translateY(-3px); box-shadow:0 18px 30px rgba(99,102,241,0.12) }

    /* Small project preview footer on left */
    .mini-proj{
      margin-top:20px;
      display:flex;flex-direction:row;gap:12px;align-items:center;
    }
    .chip{background:rgba(99,102,241,0.08); color:var(--accent); padding:6px 10px;border-radius:999px;font-weight:700;font-size:13px;border:1px solid rgba(99,102,241,0.06)}

    /* Success modal */
    .modal-backdrop{ position:fixed; inset:0; background:rgba(2,6,23,0.45); display:flex; align-items:center; justify-content:center; opacity:0; pointer-events:none; transition:opacity .18s ease; z-index:60}
    .modal-backdrop.show{ opacity:1; pointer-events:auto }
    .modal{ background:var(--card); padding:22px 20px;border-radius:14px;box-shadow:0 14px 40px rgba(2,6,23,0.25); width:90%; max-width:420px; text-align:center}
    .modal h3{font-size:20px;margin-bottom:8px}
    .modal p{color:var(--muted); margin-bottom:16px}

    /* small screens */
    @media (max-width:920px){
      .card{grid-template-columns:1fr; padding:0}
      .hero{padding:28px 20px}
      .signup{padding:20px}
      .blob.b1{ display:none }
      .blob.b2{ left:-60px; bottom:-60px }
    }

    /* tiny flourish: animated underline */
    .title-underline{
      height:6px;width:72px;border-radius:6px;margin-top:14px;background:linear-gradient(90deg,var(--accent),var(--accent-2));
      transform-origin:left; animation:grow 1.1s cubic-bezier(.2,.9,.3,1) forwards;
      opacity:0; transform:scaleX(0);
    }
    @keyframes grow{ to{opacity:1; transform:scaleX(1)} }

    /* validation red */
    .invalid{ border-color: #ef4444 !important; box-shadow: 0 6px 18px rgba(239,68,68,0.06) !important; }
  </style>
</head>
<body>

  <div class="card" role="main" aria-label="Priyanshu Portfolio Signup">
    <!-- blobs -->
    <div class="blob b1" aria-hidden="true"></div>
    <div class="blob b2" aria-hidden="true"></div>
    <div class="blob b3" aria-hidden="true"></div>

    <!-- left hero -->
    <div class="hero">
      <div style="z-index:2">
        <div class="title">Hi, I'm <span class="name title name">Priyanshu</span></div>
        <div class="title-underline" aria-hidden="true"></div>
        <div class="lead">
          A Full-Stack Web Developer who loves clean UI, Java backends and building projects that people enjoy using. I build modern web apps using Java, React, Node.js and databases — focused on performance and clarity.
        </div>

        <div class="cta-row" style="margin-top:16px">
          <a class="btn btn-primary" href="#signup" onclick="document.getElementById('name').focus();return false;">Get Started</a>
          <a class="btn btn-ghost" href="#projects" onclick="alert('Projects section coming soon!')">See Projects</a>
        </div>

        <div class="mini-proj" aria-hidden="true">
          <div class="chip">NEW</div>
          <div style="color:var(--muted);font-size:13px">Multi-Alarm Clock • Student Report Card • To-Do App</div>
        </div>
      </div>
    </div>

    <!-- right signup -->
    <aside class="signup" id="signup">
      <div>
        <div class="h2">Join Priyanshu's updates</div>
        <div class="muted">Sign up for project updates, availability, and short Java + Web tips.</div>
      </div>

      <form id="signupForm" novalidate>
        <div class="input">
          <label for="name">Full name</label>
          <input id="name" name="name" type="text" placeholder="Priyanshu Kumar" required />
          <div class="small" id="nameHelp">Use the name you prefer publicly.</div>
        </div>

        <div class="input">
          <label for="email">Email address</label>
          <input id="email" name="email" type="email" placeholder="you@example.com" required />
          <div class="small" id="emailHelp">We’ll only send occasional updates — no spam.</div>
        </div>

        <div class="input">
          <label for="role">What are you most interested in?</label>
          <input id="role" name="role" type="text" placeholder="Frontend, Backend (Java), Freelance, etc." />
        </div>

        <div style="display:flex;align-items:center;justify-content:space-between;gap:12px">
          <div class="checkbox-row">
            <input id="agree" type="checkbox" checked />
            <label for="agree" style="font-weight:600;color:var(--muted);font-size:13px;margin-left:6px">Send occasional emails</label>
          </div>
          <div style="font-size:12px;color:var(--muted)">Privacy respected</div>
        </div>

        <button type="submit" class="submit">Sign up — it's free</button>
      </form>

      <div style="margin-top:10px;font-size:13px;color:var(--muted)">Or contact me on <strong style="color:var(--accent)">LinkedIn</strong> / GitHub</div>
    </aside>
  </div>

  <!-- modal -->
  <div class="modal-backdrop" id="modal" role="dialog" aria-modal="true" aria-hidden="true">
    <div class="modal" role="document">
      <h3>You're in — Welcome!</h3>
      <p id="modalText">Thanks for signing up. I'll send updates soon.</p>
      <button class="submit" id="closeModal">Close</button>
    </div>
  </div>

  <script>
    // Simple form handling + validation + local "storage" demo
    (function(){
      const form = document.getElementById('signupForm');
      const nameInput = document.getElementById('name');
      const emailInput = document.getElementById('email');
      const roleInput = document.getElementById('role');
      const modal = document.getElementById('modal');
      const modalText = document.getElementById('modalText');
      const closeModal = document.getElementById('closeModal');

      function validateEmail(email){
        // Basic email validation (not exhaustive)
        return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
      }

      function setInvalid(el, state){
        if(state) el.classList.add('invalid'); else el.classList.remove('invalid');
      }

      form.addEventListener('submit', function(e){
        e.preventDefault();
        let name = nameInput.value.trim();
        let email = emailInput.value.trim();
        let role = roleInput.value.trim();

        let ok = true;
        if(name.length < 2){ setInvalid(nameInput,true); ok=false; } else setInvalid(nameInput,false);
        if(!validateEmail(email)){ setInvalid(emailInput,true); ok=false; } else setInvalid(emailInput,false);

        if(!ok){
          // Focus first invalid
          const firstInvalid = form.querySelector('.invalid');
          if(firstInvalid) firstInvalid.focus();
          return;
        }

        // Save to localStorage (demo) - replace with server call if you have an API
        const subs = JSON.parse(localStorage.getItem('priyanshu_subs') || "[]");
        subs.push({name, email, role, date: new Date().toISOString()});
        localStorage.setItem('priyanshu_subs', JSON.stringify(subs));

        // Show modal
        modalText.textContent = `Thanks, ${name}! We'll email you at ${email} with updates.`;
        showModal();
        form.reset();
      });

      function showModal(){
        modal.classList.add('show');
        modal.setAttribute('aria-hidden','false');
      }
      function hideModal(){
        modal.classList.remove('show');
        modal.setAttribute('aria-hidden','true');
      }

      closeModal.addEventListener('click', hideModal);
      modal.addEventListener('click', function(e){ if(e.target === modal) hideModal(); });

      // Small keyboard accessibility: Enter to submit when focused on inputs handled by form
      // Subtle entrance animation for elements
      window.addEventListener('load', ()=> {
        document.querySelectorAll('.hero > div, .signup > div').forEach((el,i)=>{
          el.style.opacity = 0;
          el.style.transform = 'translateY(10px)';
          setTimeout(()=> { el.style.transition = 'all 500ms cubic-bezier(.2,.9,.3,1)'; el.style.opacity=1; el.style.transform='translateY(0)'; }, 120 + i*120);
        });
      });

    })();
  </script>
</body>
</html>
                                                                                                                                                                                                                                                                            